# 健身教练智能体 需求文档

| 项目名称 | 健身教练智能体 (fitness_agent) |
|----------|-------------------------------|
| 文档版本 | v1.0 |
| 编写日期 | 2026-06-30 |
| 文档状态 | 已实现并通过离线验证 |

---

## 1. 项目背景

随着可穿戴设备普及，用户在健身中能够采集心率、运动强度等生理数据，但缺少能
**实时理解这些数据并据此给出个性化指导**的智能助手。传统健身 App 多为静态计划，
无法根据用户当下的身体状态动态调整，也难以在运动过程中即时纠正动作。

本项目构建一个基于大语言模型 + ReAct（思考-行动-观察）范式的健身教练智能体，
通过模拟可穿戴设备的数据流，覆盖"制定计划 → 运动监测 → 实时指导 → 效果评估 →
饮食建议"的完整闭环。

## 2. 项目目标

1. 通过（模拟的）可穿戴设备监测用户心率、运动强度等生理数据。
2. 根据用户健身目标（减脂 / 增肌 / 提升耐力）动态生成并调整训练计划。
3. 在用户运动过程中提供实时（文字）语音指导与动作纠正。
4. 训练结束后评估训练效果，并给出与目标匹配的饮食建议。
5. 跨会话记住用户健身档案，使指导随使用越来越个性化。

## 3. 用户角色与典型场景

**角色**：有明确健身目标的个人用户（健身初学者至进阶者）。

**典型场景**：
> 用户告诉智能体"我想减脂，30 岁，体重 75 公斤，新手"。智能体记录档案、生成减脂
> 计划；用户开始训练后，系统逐拍反馈心率等数据，并在心率超出燃脂区或步频偏低时
> 实时提示纠正；训练结束给出效果评级；最后输出减脂饮食建议。下次启动时，智能体
> 仍记得用户的目标与身体数据。

## 4. 功能需求

### FR-1 生理数据监测
- **FR-1.1** 系统应模拟可穿戴设备，提供心率、运动强度、配速、步频四项实时读数。
- **FR-1.2** 应支持运动前的单次静息/状态读数（`get_biometrics`）。
- **FR-1.3** 应支持运动中的连续数据流（逐拍采样，默认 12 拍）。
- **FR-1.4** 心率目标区间应按"最大心率 = 220 − 年龄"与健身目标自动计算：
  减脂 60%–70%、提升耐力 70%–80%、增肌 80%–90%。

### FR-2 训练计划生成与动态调整
- **FR-2.1** 应按健身目标（减脂/增肌/提升耐力）× 运动水平（初级/中级/高级）生成
  结构化计划（热身 / 主项 / 强度 / 频次 / 补充 / 放松）。
- **FR-2.2** 应支持口语化目标归一化（如"耐力提升""减脂计划"映射到标准目标）。
- **FR-2.3** 当掌握用户当前生理状态时，计划应**动态调整**：
  - 心率 ≥ 150 或强度"高" → 延长缓冲、追加"降低强度 + 呼吸恢复"段；
  - 恢复状态"疲劳" → 下调训练量、追加恢复性热身。
- **FR-2.4** 调整后应输出调整依据，便于用户理解。

### FR-3 运动中实时指导与动作纠正
- **FR-3.1** 运动过程中应逐拍展示生理数据流。
- **FR-3.2** 心率超出目标区间时，应即时给出文字语音指导（如"请放慢配速、调整
  呼吸"）。
- **FR-3.3** 心率低于目标区间时，应提示强度不足。
- **FR-3.4** 步频低于健康下限（160 步/分）时，应给出动作纠正提示。
- **FR-3.5** 实时监测逻辑应为规则驱动、不依赖 LLM 调用，以保证"实时"无网络延迟。

### FR-4 训练效果评估
- **FR-4.1** 训练结束应统计平均心率、峰值心率、心率达标率、实时纠正次数，输出
  训练摘要。
- **FR-4.2** 应按达标率给出效果评级（优秀 ≥80% / 良好 ≥60% / 需改进 <60%）与
  针对性改进建议。
- **FR-4.3** 评估不佳时，应支持据观察到的心率回流重新生成/调整计划，形成闭环。

### FR-5 饮食建议
- **FR-5.1** 应按健身目标给出热量方向、三餐结构建议。
- **FR-5.2** 在已知体重时，应给出每日蛋白质摄入参考量（按目标的克/公斤系数）。

### FR-6 档案记忆
- **FR-6.1** 用户透露健身目标、年龄、体重、运动水平时，应记入档案并持久化。
- **FR-6.2** 档案应跨程序重启保留（本地 JSON：`user_profile.json`）。
- **FR-6.3** 每轮思考前应将已知档案注入提示，使指导贴合用户。

## 5. 非功能需求

| 编号 | 类别 | 要求 |
|------|------|------|
| NFR-1 | 实时性 | 运动中每拍反馈不调用 LLM，确保即时响应 |
| NFR-2 | 可移植 | 纯 Python；终端统一 UTF-8 输出，兼容 Windows(GBK) 终端 |
| NFR-3 | 可配置 | LLM 凭证/地址/模型经环境变量配置，密钥不入代码 |
| NFR-4 | 健壮性 | 工具对非法/缺失参数、未知目标、网络/解析异常做容错处理 |
| NFR-5 | 可测试 | 核心逻辑层（计划/监测/评估）可离线单测，不依赖 LLM |
| NFR-6 | 可扩展 | 工具经注册表挂载，新增能力只需加函数并注册 |
| NFR-7 | 隐私 | 生理数据与档案仅存本地，不上传第三方 |

## 6. 系统架构

沿用 ReAct 四层结构，新增运动监测层：

| 模块 | 职责 |
|------|------|
| `agent.py` | ReAct 主循环；解析/分派 Action，新增 `StartWorkout[...]` 触发实时监测 |
| `llm_client.py` | OpenAI 兼容的 LLM 客户端 |
| `memory.py` | 健身档案持久化（`user_profile.json`） |
| `prompts.py` | 健身教练系统提示词与工作流程引导 |
| `tools.py` | 工具集：计划生成、生理读数、效果评估、饮食建议、档案记忆 |
| `monitor.py` | 模拟可穿戴数据流 + 规则驱动的实时监测/纠正子循环 |

**交互协议**：LLM 每轮输出一对 `Thought / Action`；Action 三类——
工具调用 `func(arg="v")`、进入运动监测 `StartWorkout[计划要点]`、结束 `Finish[答案]`。

## 7. 数据需求

- **用户健身档案**（持久化 JSON）：健身目标、年龄、体重、运动水平。
- **生理快照**（运行时）：心率、运动强度、配速、步频。
- **训练摘要**（运行时字符串）：目标、计划、平均/峰值心率、达标率、纠正次数、采样拍数。

## 8. 约束与假设

- **C-1** 可穿戴设备数据由 Python 模拟生成，不接入真实硬件 SDK。
- **C-2** "实时语音指导"以文字播报形式呈现于终端，不做真实 TTS 语音合成。
- **C-3** 需要一个 OpenAI 兼容的 LLM 服务及有效 `LLM_API_KEY`。
- **C-4** 当前支持的健身目标限定为减脂、增肌、提升耐力三类。
- **A-1** 本工具仅作健身指导参考，不构成医疗建议；特殊健康状况用户应遵医嘱。

## 9. 验收标准

1. 设置 `LLM_API_KEY` 后 `python agent.py` 能正常进入对话。
2. 端到端走查：记录档案 → 生成计划 → `StartWorkout` 实时监测（终端逐拍打印数据
   并出现超区/步频纠正）→ 效果评估 → 饮食建议 → `Finish`。
3. 计划随生理数据动态变化（高心率追加呼吸恢复段、疲劳下调训练量并附调整依据）。
4. 重启程序后 `user_profile.json` 中的档案被记住。
5. 离线单测（`tests/test_fitness_agent.py`）全部通过，且不依赖 LLM。

## 10. 后续可扩展方向（非本期范围）

- 接入真实可穿戴设备 SDK（华为/苹果/Garmin）替换模拟层。
- 接入真实 TTS 实现语音播报。
- 扩充健身目标（如康复训练、塑形）与动作库级别的姿态纠正。
- 引入多次训练的趋势分析与周期化计划。
